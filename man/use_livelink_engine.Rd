% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr-engine.R
\name{use_livelink_engine}
\alias{use_livelink_engine}
\title{Use livelink as a 'knitr' chunk engine}
\usage{
use_livelink_engine()
}
\value{
Called for its side effect. Invisibly returns \code{TRUE} if the engine was
registered, and \code{FALSE} if 'knitr' is not installed.
}
\description{
Registers a \code{livelink} engine with 'knitr', letting you turn a code chunk in a
'Quarto' or 'R Markdown' document into a shareable link. The chunk is shown as
code and \emph{not} executed locally; instead its source is encoded into a webR or
Shinylive link.

\if{html}{\out{<div class="sourceCode">}}\preformatted{```\{livelink\}
# Load the data
data(mtcars)
plot(mtcars$mpg, mtcars$wt)
```
}\if{html}{\out{</div>}}
}
\details{
Use this rather than expression input (\code{webr_repl_link({ ... })}) inside a
knitted document. 'knitr' evaluates chunks through \code{evaluate::evaluate()},
which discards source references, so comments inside a \code{{ }} expression are
lost when a document is rendered -- no \code{keep.source} setting recovers them.
The engine is handed the chunk's verbatim source, so comments survive intact.

The engine is registered automatically when livelink is loaded, provided
'knitr' is installed. You only need to call this yourself if you have reset
\code{knitr::knit_engines}.
}
\section{Chunk options}{
\describe{
\item{\code{engine.target}}{\code{"webr"} (default), \code{"shinylive-r"}, or \code{"shinylive-py"}.}
\item{\code{autorun}}{Logical. Run the code as soon as the link opens. webR only.}
\item{\code{panels}}{Character vector of webR panels to show, e.g. \code{c("editor", "plot")}.}
\item{\code{mode}}{Shinylive display mode, \code{"editor"} or \code{"app"}.}
\item{\code{filename}}{Name for the file inside the environment.}
\item{\code{link.text}}{Text for the emitted hyperlink. Defaults to \code{"Open in webR"}
or \code{"Open in Shinylive"}.}
\item{\code{link.only}}{Logical. If \code{TRUE}, emit only the link and not the source
chunk. Defaults to \code{FALSE}.}
}

Standard chunk options such as \code{echo} and \code{eval} are honored by 'knitr' as usual.
}

\examples{
\dontshow{if (requireNamespace("knitr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Normally automatic on load; call directly only after resetting knit_engines.
use_livelink_engine()
\dontshow{\}) # examplesIf}
}
